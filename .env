# NestJS backend
VITE_API_URL=http://localhost:3001/api/v1

# Supabase — DEPRECATED. The migration is complete. The supabase client stub
# in src/integrations/supabase/client.ts safely no-ops if these are unset.
# Leave them set ONLY if you still have unmigrated callers and need to fall
# back to the original Supabase project temporarily.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
# VITE_SUPABASE_PROJECT_ID=
